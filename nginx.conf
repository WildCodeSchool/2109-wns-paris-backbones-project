events {}

http {
  include mime.types;

  server {
    listen 80;

    location /graphql {
      proxy_pass http://back:4001;
    }

    location / {
      root /web-front-build;
      try_files $uri /index.html;
    }
  }
}